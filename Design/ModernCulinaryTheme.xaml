<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Color x:Key="CulinaryBackgroundColor">#FFF8F1E8</Color>
    <Color x:Key="CulinarySurfaceColor">#FFFFFFFF</Color>
    <Color x:Key="CulinarySurfaceAltColor">#FFFFEFE1</Color>
    <Color x:Key="CulinaryPrimaryColor">#FFE7663F</Color>
    <Color x:Key="CulinaryPrimaryDarkColor">#FFC74B2A</Color>
    <Color x:Key="CulinaryAccentColor">#FF2F8F6F</Color>
    <Color x:Key="CulinaryTextColor">#FF2F261F</Color>
    <Color x:Key="CulinaryMutedTextColor">#FF7A6B5F</Color>

    <SolidColorBrush x:Key="CulinaryBackgroundBrush" Color="{StaticResource CulinaryBackgroundColor}" />
    <SolidColorBrush x:Key="CulinarySurfaceBrush" Color="{StaticResource CulinarySurfaceColor}" />
    <SolidColorBrush x:Key="CulinarySurfaceAltBrush" Color="{StaticResource CulinarySurfaceAltColor}" />
    <SolidColorBrush x:Key="CulinaryPrimaryBrush" Color="{StaticResource CulinaryPrimaryColor}" />
    <SolidColorBrush x:Key="CulinaryPrimaryDarkBrush" Color="{StaticResource CulinaryPrimaryDarkColor}" />
    <SolidColorBrush x:Key="CulinaryAccentBrush" Color="{StaticResource CulinaryAccentColor}" />
    <SolidColorBrush x:Key="CulinaryTextBrush" Color="{StaticResource CulinaryTextColor}" />
    <SolidColorBrush x:Key="CulinaryMutedTextBrush" Color="{StaticResource CulinaryMutedTextColor}" />
    <SolidColorBrush x:Key="CulinaryBorderBrush" Color="#22A45B37" />

    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource CulinaryBackgroundBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="Foreground" Value="{StaticResource CulinaryTextBrush}" />
    </Style>

    <Style TargetType="Page">
        <Setter Property="Background" Value="{StaticResource CulinaryBackgroundBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="Foreground" Value="{StaticResource CulinaryTextBrush}" />
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource CulinaryTextBrush}" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="MinHeight" Value="38" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="Margin" Value="0,4,0,10" />
        <Setter Property="Foreground" Value="{StaticResource CulinaryTextBrush}" />
        <Setter Property="Background" Value="{StaticResource CulinarySurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource CulinaryBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style TargetType="PasswordBox">
        <Setter Property="MinHeight" Value="38" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="Margin" Value="0,4,0,10" />
        <Setter Property="Background" Value="{StaticResource CulinarySurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource CulinaryBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="MinHeight" Value="38" />
        <Setter Property="Padding" Value="10,6" />
        <Setter Property="Margin" Value="0,4,0,10" />
        <Setter Property="Background" Value="{StaticResource CulinarySurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource CulinaryBorderBrush}" />
    </Style>

    <Style TargetType="Button">
        <Setter Property="MinHeight" Value="42" />
        <Setter Property="Padding" Value="18,9" />
        <Setter Property="Margin" Value="0,6,0,6" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Background" Value="{StaticResource CulinaryPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource CulinaryPrimaryDarkBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="14"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource CulinaryPrimaryDarkBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.86" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ListBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <Style TargetType="ListBoxItem">
        <Setter Property="Margin" Value="0,0,0,10" />
        <Setter Property="Padding" Value="12" />
        <Setter Property="Background" Value="{StaticResource CulinarySurfaceBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="ItemBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="18"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource CulinarySurfaceAltBrush}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="#FFFFD7C4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
